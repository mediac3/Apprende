# Copie este archivo a .env y ajuste los valores.
# .env está ignorado por git (.gitignore: .env*) — nunca suba credenciales reales.
DATABASE_URL=file:../db/custom.db

# Opcionales — usados por scripts/seed.ts para contraseñas de usuarios demo:
# SEED_ADMIN_PASSWORD=cambiar
# SEED_PASSWORD=cambiar
